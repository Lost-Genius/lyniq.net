"use client";
import React from 'react';import {Link} from "@/newsroom/navigation";import type {OutputType} from '../endpoints/news_GET.schema';import s from './StoryCard.module.css';export const StoryCard=({article:a,lead=false,className}:{article:OutputType['articles'][number],lead?:boolean,className?:string})=><article className={s.card+' '+(lead?s.lead:'')+' '+(className||'')}><div className={s.meta}><span>{a.section==='Local'?'TRINIDAD & TOBAGO':a.section.toUpperCase()}</span> · {a.sourceName||'LYNIQ ORIGINAL'}</div><small>By {a.byline} · {a.publishedAt?new Date(a.publishedAt).toLocaleDateString('en-TT',{day:'numeric',month:'short',year:'numeric',timeZone:'America/Port_of_Spain'}):''}</small><h2>{a.sourceUrl?<a href={a.sourceUrl} target="_blank" rel="noopener noreferrer" data-analytics="source-story">{a.title} ↗</a>:<Link to={'/article/'+a.slug}>{a.title}</Link>}</h2><p>{a.excerpt}</p>{a.sourceUrl?<><a href={a.sourceUrl} target="_blank" rel="noopener noreferrer">Read at {a.sourceName} ↗</a>{a.sourceName?.startsWith('Global Voices')&&<small className={s.license}>Shortened feed excerpt · <a href="https://creativecommons.org/licenses/by/3.0/" target="_blank" rel="noopener noreferrer">CC BY 3.0</a></small>}</>:<Link to={'/article/'+a.slug}>Read the story →</Link>}</article>;
