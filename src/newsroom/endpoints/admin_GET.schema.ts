import superjson from 'superjson';import type {Selectable} from 'kysely';import type {Articles,Sources} from '../helpers/schema';export type OutputType={role:'owner'|'editor'|'writer',userId:number,articles:Selectable<Articles>[],sources:Selectable<Sources>[],members:{userId:number,role:string,displayName:string,email:string}[]};export async function getAdmin():Promise<OutputType>{const r=await fetch('/_api/admin');const d=superjson.parse<any>(await r.text());if(!r.ok)throw new Error(d.error);return d;}
