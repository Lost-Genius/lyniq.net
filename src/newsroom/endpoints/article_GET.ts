import {schema} from './article_GET.schema';import {publicArticle} from '../helpers/publicArticle';import superjson from 'superjson';export async function handle(r:Request){const input=schema.safeParse(Object.fromEntries(new URL(r.url).searchParams));if(!input.success)return new Response('Invalid slug',{status:400});const article=await publicArticle(input.data.slug);return new Response(superjson.stringify(article),{status:article?200:404,headers:{'Content-Type':'application/json','Cache-Control':'public,max-age=60'}})}
