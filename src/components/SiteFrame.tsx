"use client";
import React,{Suspense} from 'react';import {usePathname} from 'next/navigation';import Navbar from './Navbar';
export default function SiteFrame({children}:{children:React.ReactNode}){const p=usePathname();const news=p==='/'||['/about','/privacy','/terms','/sources','/admin'].includes(p)||p.startsWith('/article/')||p.startsWith('/newsroom/');return news?<div className="newsroom-root"><Suspense fallback={<div style={{padding:40}}>Loading Lyniq…</div>}>{children}</Suspense></div>:<><Navbar/><main className="max-w-6xl mx-auto px-4 py-6">{children}</main><footer className="border-t border-cyber-border mt-16 py-8 text-center text-cyber-muted text-sm"><p>LYNIQ COMMUNITY</p></footer></>}
