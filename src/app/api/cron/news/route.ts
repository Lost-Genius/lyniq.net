import {timingSafeEqual} from 'node:crypto';import {importFeeds} from '@/newsroom/helpers/importFeeds';export const runtime='nodejs';export const dynamic='force-dynamic';export const maxDuration=60;export async function GET(r:Request){const secret=process.env.CRON_SECRET;if(!secret)return Response.json({error:'Cron not configured'},{status:503});const actual=Buffer.from(r.headers.get('authorization')||'');const expected=Buffer.from('Bearer '+secret);if(actual.length!==expected.length||!timingSafeEqual(actual,expected))return Response.json({error:'Unauthorised'},{status:401});await importFeeds();return Response.json({ok:true})}
