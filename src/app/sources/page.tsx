import {QueryClient,dehydrate} from '@tanstack/react-query';import {publicNews} from '@/newsroom/helpers/publicNews';import Page from '@/newsroom/pages/sources';import Providers from '@/newsroom/Providers';export const dynamic='force-dynamic';export const metadata={title:'Sources & curation',alternates:{canonical:'/sources'}};export default async function Sources(){const qc=new QueryClient();await qc.prefetchQuery({queryKey:['news'],queryFn:publicNews});return <Providers state={dehydrate(qc)}><Page/></Providers>}
