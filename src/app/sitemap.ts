import type {MetadataRoute} from 'next';import {db} from '@/newsroom/helpers/db';export const dynamic='force-dynamic';export default async function sitemap():Promise<MetadataRoute.Sitemap>{const articles=await db.selectFrom('articles').select(['slug','updatedAt']).where('sourceId','is',null).where('status','in',['published','scheduled']).where('publishedAt','<=',new Date()).execute();return [...['/','/sources','/about','/privacy','/terms'].map(p=>({url:'https://lyniq.net'+p})),...articles.map(a=>({url:'https://lyniq.net/article/'+a.slug,lastModified:a.updatedAt}))]}
